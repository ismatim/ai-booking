# URL of the FastAPI backend.
# For local development (FastAPI running on default port):
NEXT_PUBLIC_API_URL=http://localhost:8000

# For Docker Compose (Next.js reaches the backend via the service name):
# NEXT_PUBLIC_API_URL=http://app:8000
